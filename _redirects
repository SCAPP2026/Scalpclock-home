# Maintenance mode — all pages redirect to the maintenance homepage
# / itself has NO rule so index.html is served directly (no loop)
# API functions (/api/*) are unaffected — CF Functions take priority over _redirects
# To restore: delete this file, rename index.backup.html → index.html, push

/dashboard      /    302
/scalpchart     /    302
/exitassistant  /    302
/pricing        /    302
/login          /    302
/learn          /    302
/faq            /    302
/terms          /    302
/privacy        /    302
/success        /    302
/debug          /    302
/replay         /    302
