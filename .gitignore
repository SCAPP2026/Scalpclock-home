# Environment / secrets — never commit these
.env
.dev.vars

# Wrangler
.wrangler/

# Nested clone (accidental git clone inside repo)
Scalpclock-home/

# Claude Code worktrees (used for background/agent sessions, not project source)
.claude/

# OS
.DS_Store
